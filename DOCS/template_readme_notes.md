# Template README Notes

Include in README:
- Purpose: neutral, SRMS-free starter with process/personas, sample project, optional BugMgmt, UI template.
- Quickstart commands (regenerate exports, open UI via file://).
- BugMgmt toggle (how to skip/remove).
- Process entrypoints (DOCS/process.md, projectplan.md, personas).
- Sample project pointer.
- PII warning and deterministic outputs note.
- Paths/config flags for scripts/issues.py.
