# Template README Notes

Include in README:
- Purpose: standalone drop-in pack with process/personas, sample project, optional BugMgmt, UI template.
- Quickstart commands (regenerate exports, open UI via file://).
- BugMgmt toggle (how to skip/remove).
- Process entrypoints (AI_first/docs/process.md, projectplan.md, personas).
- Workspace layout (AI_first/ is the drop-in folder; add product directories as needed in downstream repos).
- Project initializer (`python3 AI_first/scripts/init_project.py`) for scaffolding new projects.
- Doc watcher (`python3 AI_first/scripts/watch_docs.py`) for automatic HTML rendering.
- Sample project pointer.
- PII warning and deterministic outputs note.
- Paths/config flags for AI_first/scripts/issues.py.
