# Template README Notes

Include in README:
- Purpose: neutral, SRMS-free starter with process/personas, sample project, optional BugMgmt, UI template.
- Quickstart commands (regenerate exports, open UI via file://).
- BugMgmt toggle (how to skip/remove).
- Process entrypoints (support/docs/process.md, projectplan.md, personas).
- Workspace layout (src/tests/config/data/assets/scripts/docs; keep support/ for tooling).
- Project initializer (`python3 support/scripts/init_project.py`) for scaffolding new projects.
- Doc watcher (`python3 support/scripts/watch_docs.py`) for automatic HTML rendering.
- Sample project pointer.
- PII warning and deterministic outputs note.
- Paths/config flags for support/scripts/issues.py.
